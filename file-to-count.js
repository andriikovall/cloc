if (a === 1) { //
  console.log('hello');
} else if (b === 2) { // 
  console.log('world');
  const a = d === 3 ? 'foo' : 'bar'; //
  try { //
    const a = d === 3 ? 'foo' : 'bar'; //
  } catch(e) {} //
  if (a === 1) { //
    console.log('hello');
  } else if (b === 2) { // 
    console.log('world');
    const a = d === 3 ? 'foo' : 'bar'; //
    try { //
      const a = d === 3 ? 'foo' : 'bar'; //
    } catch(e) {} //
  }
}

const a = d === 3 ? 'foo' : 'bar'; //
try { //

} catch(e) {} //

switch (3) { //
  case 2: //
  case 3: //
  console.log(213);
}
// 18

for (let i = 0; i < 10; i++) { //
  console.log(i)
};

for (const a in b) { //
  console.log(a);
}

for (const a of b) { //
  console.log(a);
}

do {

} while(false); //

while (false) { //

}
