if (a === 1) { //
  console.log('hello');
} else if (b === 2) { // 
  console.log('world');
  const a = d === 3 ? 'foo' : 'bar'; //
  try { //
    const a = d === 3 ? 'foo' : 'bar'; //
  } catch(e) {} //
  if (a === 1) { //
    console.log('hello');
  } else if (b === 2) { // 
    console.log('world');
    const a = d === 3 ? 'foo' : 'bar'; //
    try { //
      const a = d === 3 ? 'foo' : 'bar'; //
    } catch(e) {} //
  }
}

const a = d === 3 ? 'foo' : 'bar'; //
try { //

} catch(e) {} //

switch (3) { //
  case 2: //
  case 3: //
  console.log(213);
}
